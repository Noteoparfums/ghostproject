# Project Rules

## Proactive Execution
- Always proactively execute commands, setup steps, migrations, seeds, and local servers rather than asking the user to do them.
- If a configuration or environment file needs modification, apply the changes directly.
- Only stop and ask for user interaction when input is strictly required (e.g., when missing confidential credentials or when design decisions require confirmation).
